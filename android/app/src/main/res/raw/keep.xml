<?xml version="1.0" encoding="utf-8"?>
<!-- The notification icon is only named from Dart, so the release build
     would otherwise drop it as an unused resource. -->
<resources
    xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_notification" />
